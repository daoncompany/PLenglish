# 문의 폼 → 지메일 즉시 발송 연동 가이드

홈페이지 문의 폼을 구글 계정에 연결하면 이렇게 동작합니다.

```
방문자가 폼 작성 → [문의 보내기] 클릭
        ↓
  ① 학원 지메일로 내용 그대로 즉시 발송  (그 메일에 답장하면 문의자에게 바로 회신됨)
  ② 구글 시트에 한 줄씩 자동 기록        (접수일시 · 이름 · 이메일 · 연락처 · 내용)
        ↓
  방문자 화면에 "문의가 정상적으로 접수되었습니다" 표시
```

- 비용 : **무료** (무료 구글 계정 기준 하루 100통까지 발송 가능)
- 준비물 : 학원 구글(지메일) 계정 하나
- 소요 시간 : 약 10분

---

## 1단계 — 구글 시트 만들기

1. 학원 구글 계정으로 로그인 후 [sheets.new](https://sheets.new) 접속
2. 파일 이름을 **`PL어학원 홈페이지 문의접수`** 로 변경

> 문의 내역이 이 시트에 쌓입니다. 시트 탭은 스크립트가 자동으로 만들어 주니 그냥 두시면 됩니다.

## 2단계 — Apps Script 열기

1. 방금 만든 시트에서 상단 메뉴 **확장 프로그램 → Apps Script** 클릭
2. 새 탭에 코드 편집기가 열리고 `function myFunction() { }` 같은 기본 코드가 보입니다
3. **기본 코드를 전부 지우고**, 이 폴더의 **`apps-script/Code.gs`** 파일 내용을 전부 복사해서 붙여넣기

## 3단계 — 학원 정보 입력

붙여넣은 코드 맨 위 세 줄만 수정합니다.

```javascript
var MAIL_TO   = 'plinstitute@gmail.com';   // ← 문의를 받을 실제 메일 주소
var MAIL_FROM = 'PL어학원 홈페이지';       // ← 그대로 두셔도 됩니다
var SHEET_NAME = '문의접수';                // ← 그대로 두셔도 됩니다
```

- 여러 명이 함께 받으려면 콤마로 구분 : `'a@gmail.com, b@gmail.com'`
- 저장 : 상단 💾 아이콘 또는 `Ctrl + S`

## 4단계 — 배포하기 (가장 중요)

1. 오른쪽 위 **배포 → 새 배포** 클릭
2. 왼쪽 톱니바퀴(⚙️ 유형 선택) → **웹 앱** 선택
3. 아래처럼 설정합니다

   | 항목 | 설정값 |
   |---|---|
   | 설명 | `PL 문의접수` (아무 이름이나) |
   | 다음 사용자로 실행 | **나 (본인 계정)** |
   | 액세스 권한 | **모든 사용자** ← 반드시 이걸로 |

   > "모든 사용자"로 해야 홈페이지 방문자가 로그인 없이 문의를 보낼 수 있습니다.

4. **배포** 클릭 → 권한 승인 창이 뜹니다
   - `액세스 승인` → 계정 선택 → **"Google에서 확인하지 않은 앱"** 경고가 나오면
     **고급 → (프로젝트 이름)(으)로 이동 → 허용** 순서로 진행하세요.
     (본인이 만든 스크립트라서 나오는 정상적인 경고입니다.)
5. 배포가 끝나면 **웹 앱 URL** 이 나옵니다. 이런 형태입니다.

   ```
   https://script.google.com/macros/s/AKfycb...................../exec
   ```

   이 주소를 복사해 주세요.

## 5단계 — 홈페이지에 주소 붙여넣기

`assets/js/contact.js` 파일을 열면 맨 위에 이 부분이 있습니다.

```javascript
window.PL_CONTACT = {
  endpoint: '',                          // ← 여기에 4단계에서 복사한 주소를 붙여넣기
  mailTo: 'pl-academy@example.com',      // ← 학원 메일 주소
  tel: '000-0000-0000',                  // ← 학원 대표 전화번호
};
```

이렇게 바꿔주면 끝입니다.

```javascript
window.PL_CONTACT = {
  endpoint: 'https://script.google.com/macros/s/AKfycb...../exec',
  mailTo: 'plinstitute@gmail.com',
  tel: '02-000-0000',
};
```

> `endpoint` 를 **비워두면** 예전처럼 방문자의 메일 앱을 여는 방식으로 동작합니다.
> 즉, 주소를 넣기 전까지도 홈페이지는 정상적으로 작동합니다.

## 6단계 — 확인

1. 홈페이지 `문의하기` 페이지에서 실제로 한 번 작성해 제출
2. 학원 지메일에 `[홈페이지 문의] OOO님` 메일이 도착하는지 확인
3. 구글 시트 `문의접수` 탭에 한 줄이 추가됐는지 확인

메일이 안 온다면 Apps Script 편집기에서 함수 목록의 **`테스트발송`** 을 실행해 보세요.
메일이 오면 배포 설정 문제(액세스 권한이 "모든 사용자"인지), 안 오면 메일 주소 오타입니다.

---

## 알아두실 점

| 항목 | 내용 |
|---|---|
| 발송 한도 | 무료 구글 계정 하루 100통 / Workspace 계정 하루 1,500통 |
| 스팸 차단 | 사람 눈에 안 보이는 함정 필드가 있어 자동 스팸봇 대부분 걸러집니다 |
| 회신 방법 | 받은 문의 메일에 **그대로 답장**하면 문의자 메일로 회신됩니다 |
| 코드 수정 후 | Apps Script를 고쳤다면 **배포 → 배포 관리 → 편집(✏️) → 버전: 새 버전 → 배포** 를 해야 반영됩니다 |
| 전송 실패 시 | 방문자 화면에 "메일로 보내기 / 전화 상담" 버튼이 대신 나타나 문의가 끊기지 않습니다 |
| 개인정보 | 문의 내역이 학원 구글 계정에만 쌓입니다. 외부 서비스를 거치지 않습니다 |
